{
  "name": "enkl-wiki",
  "version": "1.0.0",
  "private": true,
  "description": "100% client-side Content Management System that runs offline and stores pages as Markdown in JSON",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.0"
  },
  "dependencies": {
    "prismjs": "^1.30.0"
  }
}
